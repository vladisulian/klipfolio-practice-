import "./Header.scss";
import { Navigation } from "./Navigation/Navigation";

const Header = () => {
  return (
    <header className="container">
      <Navigation />
    </header>
  );
};

export default Header;
